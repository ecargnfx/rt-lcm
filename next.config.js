/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
      domains: ['replicate.delivery'], // Add the domain of your image here
    },
    // ... any other existing configurations
  }
  
  module.exports = nextConfig;
  